mixed-port: 7890
allow-lan: false
mode: rule
log-level: info

proxies:
  - name: Example-Proxy
    type: socks5
    server: 127.0.0.1
    port: 1080

proxy-groups:
  - name: PROXY
    type: select
    proxies:
      - Example-Proxy

rules:
  - MATCH,PROXY

